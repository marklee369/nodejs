services:
  - type: web
    name: ssh-gateway-backend
    runtime: node
    plan: starter
    region: singapore
    buildCommand: pnpm install --frozen-lockfile
    startCommand: pnpm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production