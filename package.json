{
  "name": "ssh-gateway-backend",
  "version": "1.0.0",
  "description": "SSH WebSocket gateway — backend only (SSRF-guarded SSH proxy over WebSocket). Deploy separately from the frontend.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=24.21.0"
  },
  "packageManager": "pnpm@12.5.1",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "helmet": "^8.3.0",
    "ssh2": "^1.17.0",
    "ws": "^8.21.3",
    "zod": "^4.6.5"
  }
}
